import 'package:flutter/material.dart';

class ScholarshipTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Scholarship Notice"),
      ),
    );
  }
}
